---
includes:
  # Un-comment while developing.
  # - server_dev.yaml
  - package://runtimepy/factories.yaml

app:
  - runtimepy.net.server.app.setup

ports:
  - {name: runtimepy_http_server, type: tcp}
  - {name: runtimepy_websocket_server, type: tcp}

servers:
  - factory: runtimepy_http
    kwargs: {port: "$runtimepy_http_server"}

  - factory: runtimepy_websocket_json
    kwargs: {host: "0.0.0.0", port: "$runtimepy_websocket_server"}
